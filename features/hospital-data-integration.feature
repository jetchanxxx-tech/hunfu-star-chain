# language: zh-CN
功能: 医院数据对接
  通过前置机从医院 HIS/LIS/PACS/EMR 系统采集数据,
  经字段映射、数据清洗后写入平台健康报告库,
  支持定时拉取和增量更新。

  背景:
    假设 医院 "妇幼保健院" 已完成前置机部署
    并且 系统已配置该医院的数据源连接信息
    并且 前置机已获得 HIS/LIS/PACS/EMR 的只读数据库权限

  场景: 配置医院数据源
    当 管理员在后台进入 "数据源管理" 页面
    并且 新增数据源, 填写:
      | 字段         | 值                          |
      | 医院编码     | FEY-001                     |
      | 医院名称     | 妇幼保健院                  |
      | 数据源类型   | HIS / LIS / PACS / EMR      |
      | 连接方式     | 专线/HTTPS                  |
      | 连接地址     | 10.0.1.100:3306             |
      | 数据库名     | his_db                      |
      | 只读账号     | huifu_readonly              |
      | 数据格式     | HL7v2 / FHIR R4 / 自定义SQL |
    并且 点击 "测试连接"
    那么 系统尝试连接并返回连接测试结果
    当 连接成功
    那么 管理员点击 "保存" 完成数据源注册

  场景: 配置字段映射规则
    当 管理员为数据源 "FEY-001-HIS" 配置字段映射
    那么 可以定义映射规则:
      | 平台字段      | 医院字段(例)         | 转换规则     |
      | member_id     | PATIENT_ID           | 通过身份证号匹配 |
      | report_type   | EXAM_TYPE            | LAB→lab, US→imaging |
      | report_date   | REPORT_DATE          | yyyyMMdd→yyyy-MM-dd |
      | summary       | EXAM_RESULT          | JSON结构化提取  |
      | abnormal_flags| ABNORMAL_INDICATOR   | H→异常标记     |
    并且 支持映射规则测试: 输入医院原始数据, 预览转换结果

  场景: 定时拉取检验报告(HIS/LIS)
    当 系统定时任务(每30分钟)执行数据拉取
    那么 查询 HIS/LIS 中最近30分钟的新增/更新的检验报告
    并且 逐条应用字段映射规则
    并且 数据清洗:
      | 清洗规则     | 说明                       |
      | 去重         | 同一 report_no 不重复导入  |
      | 脱敏         | 患者姓名/手机号 SHA256 哈希 |
      | 异常识别     | 指标超出参考范围自动标记   |
      | 数据验证     | 必填字段缺失的记录写入死信队列 |
    并且 将清洗后的数据写入 health_reports 表(source=his/lis)
    并且 记录同步日志: 数据源、拉取时间、总条数、成功条数、失败条数

  场景: 拉取影像报告(PACS)
    当 系统定时任务执行 PACS 数据拉取
    那么 获取新增的 B超/影像检查报告
    并且 报告原文(PDF)上传至对象存储
    并且 health_reports 中 file_url 指向对象存储路径
    并且 report_type 设为 "imaging"

  场景: 拉取出院小结(EMR)
    当 系统定时任务执行 EMR 数据拉取
    那么 获取新增的出院小结和就诊记录
    并且 提取结构化字段: 入院日期、出院日期、诊断、治疗摘要
    并且 存储到 health_reports (report_type=discharge)
    并且 同时触发时间轴事件更新(如分娩事件)

  场景: 增量更新——已有报告更新
    假设 系统中已存在报告号 "RPT-2025001" 的报告
    当 医院端该报告数据发生变更(如追加审核结果)
    那么 系统拉取时识别到 report_no 已存在
    并且 比较更新时间, 如果医院端更新则覆盖
    并且 更新记录记录版本号 +1

  场景: 异常数据处理
    当 拉取到无法解析的记录(字段缺失/格式错误)
    那么 该记录不写入 health_reports 主表
    并且 写入 dead_letter_queue 表,记录:
      | 字段         | 值                     |
      | source       | FEY-001-HIS            |
      | raw_data     | 原始数据JSON           |
      | error_reason | 必填字段 member_id 为空 |
      | created_at   | 当前时间               |
    并且 管理员可在死信队列页面查看并手动处理

  场景: 查看数据同步仪表板
    当 管理员进入 "数据同步监控" 页面
    那么 可以看到:
      | 指标           | 说明                         |
      | 各数据源状态   | 正常/异常/延迟               |
      | 最近同步时间   | 每个数据源的最后成功同步时间 |
      | 今日同步量     | 各类型的导入数量             |
      | 待处理死信     | dead_letter_queue 中未处理数 |
      | 同步失败告警   | 连续失败3次自动通知管理员    |

  场景: 手动触发单条数据导入
    当 管理员在死信队列中点击某条记录
    并且 手动修正字段映射后点击 "重新导入"
    那么 系统使用修正后的数据重新执行导入流程
    并且 成功则从死信队列中移除
