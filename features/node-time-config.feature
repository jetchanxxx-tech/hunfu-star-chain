# language: zh-CN
功能: 节点时间后台配置
  医院管理员可以配置全周期时间轴的各个节点及其默认时间范围和提醒规则,
  各医院可以有独立的配置覆盖。

  背景:
    假设 已登录为医院管理员
    并且 系统存在默认节点模板 "NT检查" 默认时间范围 "孕11-13周+6天"
    并且 系统存在默认节点模板 "早唐筛查" 默认时间范围 "孕15-16周"
    并且 系统存在默认节点模板 "糖耐量" 默认时间范围 "孕24-28周"
    并且 系统存在默认节点模板 "产后42天复查" 默认时间范围 "产后42天"
    并且 系统存在默认节点模板 "儿童保健" 默认时间范围 "出生后2月龄、3月龄"

  场景: 查看默认节点模板列表
    当 管理员访问节点配置页面
    那么 可以看到所有默认节点模板
    并且 每个节点显示: 名称、默认时间范围、提醒提前天数、状态

  场景: 修改默认节点模板的时间范围
    当 管理员将 "NT检查" 的时间范围修改为 "孕11-14周"
    并且 保存修改
    那么 系统提示 "保存成功"
    并且 "NT检查" 的默认时间范围更新为 "孕11-14周"
    并且 已有医院的覆盖配置不受影响

  场景: 为特定医院创建节点时间覆盖
    当 管理员选择医院 "妇幼保健院"
    并且 将 "NT检查" 开始时间设为 "孕12周" 结束时间设为 "孕13周+6天"
    并且 设置提醒提前天数为 "7天"
    并且 保存覆盖配置
    那么 该医院下 "NT检查" 使用覆盖后的时间配置
    并且 其他未覆盖的医院继续使用默认配置

  场景: 禁用某个节点
    当 管理员在默认模板中禁用 "儿童保健" 节点
    那么 系统提示 "该节点已禁用，是否同步到所有使用默认配置的医院？"
    当 管理员确认禁用
    那么 所有使用默认配置的医院不再显示该节点
    并且 已有覆盖配置的医院不受影响

  场景: 新增自定义节点模板
    当 管理员点击 "新增节点"
    并且 填写节点名称为 "四维彩超"
    并且 设置默认时间范围为 "孕22-26周"
    并且 设置提醒提前天数为 "3天"
    并且 保存
    那么 节点模板列表中出现 "四维彩超"
    并且 所有使用默认配置的医院自动获得该节点

  场景: 设置节点的参考范围说明
    当 管理员编辑 "NT检查" 节点
    并且 添加备注说明 "NT值正常范围 <2.5mm"
    并且 保存
    那么 该节点的详情中显示备注说明

  场景: 节点时间范围与提醒逻辑的边界情况
    当 管理员设置提醒提前天数大于时间范围跨度
    那么 系统提示 "提醒天数不能超过时间范围跨度"
    并且 修改被拒绝

  场景: 批量导入医院节点配置
    当 管理员上传包含多医院配置的 Excel 文件
    并且 文件格式正确
    那么 系统逐条校验并导入
    并且 返回导入结果汇总: 成功N条、失败M条及失败原因
