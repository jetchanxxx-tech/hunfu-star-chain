# language: zh-CN
功能: AI 智能语音电话
  对接第三方语音服务平台,由系统自动触发或管家手动发起外呼,
  语音内容由 AI 生成,支持多供应商可插拔切换。

  背景:
    假设 系统已配置语音服务供应商 "阿里云语音" 为默认供应商
    并且 会员 "小王" 已绑定手机号, 产后第42天
    并且 系统自动生成了 "产后42天电话回访" 任务

  场景: 系统自动触发语音回访
    当 产后42天回访任务状态变为 "in_progress"
    并且 任务配置为 "自动语音外呼"
    那么 系统调用语音供应商 API 发起外呼:
      | 参数         | 值                                      |
      | 被叫号码     | 小王的绑定手机号                        |
      | 语音模板     | postpartum_42day_followup               |
      | AI 话术      | 由 AI 模块根据会员档案动态生成个性化内容 |
      | 任务ID       | task_123                                |
      | 最大重试次数 | 3次(间隔30分钟)                         |
    并且 语音通话记录写入 voice_call_logs 表

  场景: AI 通话内容个性化
    当 AI 生成小王的话术时
    那么 话术中包含从会员档案提取的个性化内容:
      | 变量           | 来源                | 示例值       |
      | 称呼           | 会员昵称            | 小王         |
      | 分娩日期       | 时间轴事件          | 4月1日       |
      | 分娩方式       | 出生报告摘要        | 顺产         |
      | 宝宝性别       | 出生报告摘要        | 女宝宝       |
      | 42天复查提醒   | 时间轴计算          | 5月13日前    |
    并且 话术由 LLM 模板引擎渲染

  场景: 管家手动发起语音外呼
    当 张管家在任务详情页点击 "语音外呼"
    那么 系统弹出确认对话框 "确认拨打 小王 的电话？"
    当 张管家确认
    那么 系统使用管家绑定的坐席号码发起外呼
    并且 先呼叫管家, 管家接通后再呼叫会员(双呼模式)
    或者 使用 AI 直接外呼会员(机器人模式)

  场景: 通话结果回调处理
    当 语音供应商通过回调 URL 通知通话结果
    那么 系统解析回调数据:
      | 回调字段     | 处理方式                 |
      | 通话状态     | 更新 voice_call_logs     |
      | 通话时长     | 记录时长用于计费         |
      | 按键交互     | 记录用户按键选择         |
      | 语音文本转录 | 存储到通话记录的 transcript 字段 |
      | 意向标签     | AI 分析对话内容打标签    |
    当 通话成功
    那么 关联的随访任务自动标记为 "completed"
    当 通话失败(未接通/拒接/空号)
    那么 任务状态保持 "in_progress"
    并且 记录失败原因, 安排重试或转为人工跟进

  场景: 切换语音供应商
    假设 系统同时配置了 "阿里云语音" 和 "腾讯云语音"
    当 管理员在后台将默认供应商切换为 "腾讯云语音"
    那么 后续所有语音外呼通过腾讯云发起
    并且 正在进行中的通话不受影响(继续使用阿里云)
    并且 历史通话记录保留供应商字段用于追溯

  场景: 语音模板管理
    当 管理员进入 "语音模板配置" 页面
    那么 可以管理语音模板:
      | 模板编码                     | 模板名称         | 类型    | 状态   |
      | postpartum_42day_followup   | 产后42天回访     | 自动    | 审核通过 |
      | early_pregnancy_reminder   | 早孕到院提醒     | 自动    | 审核通过 |
      | appointment_confirm        | 预约确认         | 自动    | 审核通过 |
      | manual_custom              | 手动外呼(自定义) | 手动    | 可用   |
    并且 每个模板可以预览 AI 生成的示例话术

  场景: 通话记录查询与统计
    当 管理员进入 "语音通话记录" 页面
    那么 可以按以下条件筛选:
      | 筛选条件   | 说明            |
      | 会员       | 按会员搜索      |
      | 任务ID     | 关联的随访任务  |
      | 通话状态   | 成功/失败/未接  |
      | 时间范围   | 起止时间        |
      | 供应商     | 阿里云/腾讯云   |
    并且 统计面板显示:
      | 指标         | 说明           |
      | 总外呼次数   | 指定时间范围   |
      | 接通率       | 成功/总数      |
      | 平均通话时长 | 成功通话的平均 |
      | 供应商用量   | 各供应商外呼量 |

  场景: 空号/拒接处理策略
    当 系统检测到 3 次外呼均未接通
    那么 自动标记任务需要人工跟进
    并且 向管家发送企业微信通知 "小王产后回访3次未接通,请人工跟进"
    并且 任务备注中记录三次呼叫的详细结果

  场景: 语音呼叫频率限制
    当 同一会员在 24 小时内已收到 2 次自动语音外呼
    并且 系统尝试发起第 3 次自动外呼
    那么 系统拒绝发起
    并且 记录原因 "超出日呼叫频率限制"
    并且 将本次外呼延迟到次日执行
